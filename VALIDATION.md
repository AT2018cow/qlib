# Qlib 量化策略验证记录（2026-09）

## 环境与数据

- 计算：Modal serverless（A10G GPU / CPU 容器），入口 `modal_qlib_cn_a10g.py`
- 数据：chenditc/investment_data 每日 release（A股日频，qlib 格式），Volume `qlib-cn-data`
- 股票池：csi300 / csi500（含历史上全部成分股，按时间区间过滤），benchmark SH000300/SH000905
- 训练区间：recent 模式 2021-2024 训练 / 2025 验证 / 2026-01~09 回测；long_train 模式 2016-2024 训练

## 实验矩阵与结论

### 1. 标签周期（影响最大）

| 标签 | 模型 | Rank IC | 有成本超额(2026) |
|---|---|---|---|
| 2 日收益 | LGB+Alpha158 | 0.029 | -7.6% |
| 2 日收益 | GRU+Alpha158 (csi300) | 0.040 | -3.3% |
| **20 日收益** | **LGB+Alpha158** | **0.114** | **+1.7%** |
| **20 日收益** | **LGB+Alpha360** | **0.105** | **+6.5%** ⭐ |

结论：**标签周期是最大杠杆**。2 日标签噪声过大（IC≈0.03），20 日标签 IC 提升 3-4 倍。

### 2. 股票池 / 特征 / 模型

| 实验 | Rank IC | 结论 |
|---|---|---|
| csi300 → csi500 | 持平 | 无提升 |
| Alpha158 → Alpha360 | 0.114→0.105（IC 略降） | **回测收益大幅提升**（+1.7%→+6.5%），360 特征信号更均衡 |
| GRU/ALSTM vs LGB | LGB 明显更优 | RNN 在 20 日标签下训练不充分（early stop 早） |

### 3. 调仓 / 持仓（组合层）

| 实验 | 有成本超额 | 结论 |
|---|---|---|
| top50 每日微调（n_drop=2） | +6.5% | ✅ 最优 |
| top20 集中持仓 | +4.1% | ❌ 头部信号不更准，集中反而差 |
| 每 20 日硬调仓（PeriodicTopk） | -8.6% | ❌ 调仓点碰巧差，8 个月样本仅 ~8 次调仓 |

结论：**TopkDropoutStrategy 每日微调 + n_drop=2 在 20 日信号下实际换手低**，是正确组合方式。

### 4. Walk-forward 验证（1.7 年）

训练 2016-2023 / 回测 2025-01~2026-09：Rank IC 0.075（仍显著）、有成本超额 +1.0%（2025 基准大涨 +19%，跑赢大涨市困难）。**信号真实但收益衰减——公开方法年化超额 1-3% 是常态**。

### 5. 超参搜索 / 多周期融合 / 基本面因子（均为负面结果）

| 实验 | 结果 | 结论 |
|---|---|---|
| LGB 超参搜索 40 组 | Top1 0.1064 vs 默认 0.105 | 超参不是瓶颈，官方参数已近最优 |
| 20日+60日标签融合 | 有成本 -4.7% | 60 日标签模型是噪声源，不可行 |
| Alpha158+6 基本面字段（ROE/毛利率/PE/PB） | 无提升甚至下降 | 季度信息与 2 日标签周期错配；20 日标签下 PIT 粗糙（固定+4月）负贡献 |

## 固化最优配置（当前最佳）

```bash
# 每日信号（推荐入口：--best 组合自动带上 long_train=True，与验证模型一致）
modal run modal_qlib_cn_a10g.py --best --daily --topk 50

# 手动等价写法（注意必须带 --long-train，否则是短训练模型，程序会打印警告）
modal run modal_qlib_cn_a10g.py --daily --model lgb360 --label20 --long-train --topk 50

# 完整训练+回测（PortAnaRecord 输出 IC/回撤/超额）
modal run modal_qlib_cn_a10g.py --best
```

- 模型：**LightGBM + Alpha360 + 20 日标签（Ref($close,-20)/$close-1）**
- 训练：2016-2024（long_train），csi500 全成分（1802 只，按区间过滤）
- 组合：TopkDropoutStrategy topk=50, n_drop=2，每日微调（实际换手低）
- 信号后处理：剔除当日涨/跌停（≥9.5%）
- 2026-01~09 结果：Rank IC 0.105 / Rank ICIR 0.75 / 无成本超额 +8.4% / 有成本超额 +6.5%

## 工作路线图（全局视角，2026-09-16 制定）

> 资源口径：Modal Starter（并发容器 100 / GPU 并发 10 / 月免费额度 $30）。
> GPU 项标注 ⚡；其余全 CPU。成本为 Modal 计算费（不含人工开发时间）。

### P0 — 当前配置证据补强（全 CPU，合计 ~$0.85，并行 ~30 分钟）

| # | 工作 | 方法 | 容器 | 成本 | 必要性 |
|---|---|---|---|---|---|
| 1 | n_drop 网格（1/2/3/5） | 4 次回测 | ≤4 × 8核32G | $0.30 | 中：官方未覆盖 20 日场景，成本低 |
| 2 | long_train 直接 A/B【可选】 | 2 次训练+回测 | 2 × 8核32G | $0.25 | 低：verify 已隐含验证 |
| 3 | 10 分组单调性检验 | 训练 1 次+分组收益 | 1 × 8核32G | $0.16 | **高：官方从不提供，因子有效性金标准** |
| 4 | 分月归因（超额是否集中于个别月份） | 只读已有 report | 1 × 4核8G | $0.02 | **高：官方不检验收益来源** |
| 5 | IC 衰减曲线（1/5/10/20/40/60 日） | 训练 1 次+多周期 IC | 1 × 8核32G | $0.16 | **高：官方只报固定标签期 IC** |
| 6 | 多 seed 稳定性【可选：LGB 树模型 seed 敏感度低】 | 3 次训练 | 3 × 8核32G | $0.25 | 低 |

### P1 — QLib 量价能力增量 + 进攻性优化（全 CPU，合计 ~$2.6，约 2 小时）

| # | 工作 | 方法 | 容器 | 成本 |
|---|---|---|---|---|
| 7 | Alpha158+Alpha360 特征并集（518 维，信息合并） | 拼接 handler + 1 次训练回测 | 1 × 8核32G | $0.19 |
| 7b | **双 LGB 变体分数融合**（模型投票：两个强变体分数加权平均，区别于 7 的信息合并；集成失败教训是 RNN 拖累，强×强融合未测） | 2 次训练 + 融合回测 | 2 × 8核32G | $0.30 |
| 7c | **组合构造优化**（等权 vs 分数加权 vs 行业分散约束——信号到钱的最后一公里） | 训练 1 次 + 3 种组合回测 | 1 × 8核32G | $0.32 |
| 8 | 流动性过滤（剔除成交额后 20%） | ExpressionDFilter + 1 次训练回测 | 1 × 8核32G | $0.16 |
| 9 | 成本敏感性 + 开盘价成交 | 训练 1 次 + 3 次回测 | 1 × 8核32G | $0.32 |
| 10 | 标签周期补测（10/40 日）【可选，官方盲区边际信息少】 | 2 次训练回测 | 2 × 8核32G | $0.32 |

### P1.5 — RNN 公正审判 ⚡（GPU，~$3-5，约 1 小时）【可选：官方+我们双重证据 RNN 弱，翻身概率低】

| # | 工作 | 方法 | 容器 | 成本 |
|---|---|---|---|---|
| 11 | GRU/ALSTM 在 20 日标签下的单独评估（lr/early_stop/hidden_size 专门调参），补齐证据缺口；翻身则纳入集成 | 训练×调参轮 6-10 次 | 2-3 × A10G | $3-5 |

### P2 — 系统性升级（全 CPU，~$9）

| # | 工作 | 方法 | 容器 | 成本 |
|---|---|---|---|---|
| 12 | 超参搜索 v2：优化目标改为**有成本超额收益**（Rank IC ≠ 赚钱，Alpha158/360 实证：IC 0.114 赚 1.7% vs IC 0.105 赚 6.5%；缩减至 50 组——官方超参在 IC 维度已近优） | 50 组"训练+回测" | 8 × 16核16G | $8 |
| 13 | 滚动 walk-forward（每季度重训 1 次，8 个季度） | RollingGen 或手写循环 | 8 × 8核32G | $0.92 |

### P3 — 信息维度与重型模型（新数据接入 + ⚡GPU）

| # | 工作 | 价值 | 依赖 | 成本 |
|---|---|---|---|---|
| 14 | **消息面**：北向资金/融资融券/龙虎榜因子（akshare） | A 股特有资金流 alpha，真实信息增量（官方完全没有） | 复用 build_fund_factors 模式 | CPU ~$2 + 开发 |
| 15 | **HIST**（概念层次图模型） | 捕捉概念/板块协同——LGB 学不到的股票间关系维度；官方基准（2 日标签）中排名靠前，20 日场景待验证 | akshare 拉行业/概念数据 | ⚡ $5-10 |
| 16 | **TRA**（多模式路由） | 直接针对实测痛点：市场切换导致样本外衰减 | 长训练数据已有 | ⚡ $5-10 |
| 17 | GATs / Transformer / TFT【砍掉，除非 15/16 出结果：官方 benchmark 已系统测过全部重模型，无一显著超越 LGB】 | — | — | — |
| 18 | 基本面 PIT 精修（真实披露日期） | 修正 +4 月近似，基本面因子重试 | 披露日期数据 | CPU ~$2 + 开发 |
| 19 | RD-Agent（Azure VM + Azure OpenAI） | LLM 自动因子挖掘，构建在 Qlib 之上 | Azure credits、Docker、LLM API | Azure VM ~$300/月 |
| 20 | 组合优化 EnhancedIndexingModel【降级：P1-7c 轻量组合优化已覆盖主要收益】 | — | — | — |

### 依赖与推进原则

- **P0 → P1 顺序执行**：P0-3/4/5（单调性/归因/衰减）是地基——若信号被证伪，后续进攻全部暂停
- P1 内 7b/7c（双变体融合/组合构造）为新增进攻项，与验证项并行不冲突
- P2-13（滚动验证）优先级高于 P2-12（超参 v2）：先确认真，再搜最优
- P3 按 14→15→16 顺序（消息面与 HIST 是真实信息增量；17 重模型群测已砍）
- 每个实验落地后回写本文档；**实盘前固定流程：`verify_integrity` + 当日信号 + 操作手册纪律**

### 预算总览（Starter 限额：100 容器 / 10 GPU / $30 月免费额度）

| 层级 | 必做项成本 | GPU 占用 | 容器峰值 |
|---|---|---|---|
| P0 核心（3/4/5 + 1） | ~$0.65 | 0/10 | 4/100 |
| P1 全部（7/7b/7c/8/9，10 可选） | ~$1.3 | 0/10 | 2/100 |
| P1.5 RNN（可选） | $3-5 | 3/10 | 3/100 |
| P2 | ~$9 | 0/10 | 8/100 |
| P3-14/15/16 | ~$12 + 开发 | ≤3/10 | 少量 |
| **必做合计** | **~$23**（含可选 ~$27） | 峰值 3/10 | 峰值 8/100 |

## 资源使用规范（Modal）

- Starter 计划：并发容器 100、GPU 并发 10
- **GPU 只挂给 RNN 模型（GRU/ALSTM/集成）**；LGB/数据/超参搜索全走 CPU 容器（挂载未使用 = 按挂载计费，浪费）
- 超参搜索等并行任务：`TUNE_WORKERS=8`（8 并发 × 16 核）
- 长任务建议普通 `modal run`（本地保持连接），不要 detach（本地断开后输入会被平台取消）

## 已知风险与局限（资金安全相关，务必阅读）

1. **复权因子的潜在前视**：chenditc 数据的 `$factor` 复权因子的生成时点未经我们审计。若因子按"最新价格回算全部历史"，回测收益会被轻微高估（业界称 restatement bias）。**缓解**：结论以 walk-forward（训练截止早于回测期）为准，且实盘小仓位跟踪是最终校验。
2. **回测成交价 vs 实际执行**：回测用当日收盘价成交（`deal_price: close`），实盘按次日开盘价执行，存在隔夜跳空偏差。
3. **并列分数与边界不稳定**：LGB 输出存在并列分数（叶子值离散），top50 截断边界在并列处的选择对排序扰动敏感——每日榜单可能有几只边界股出入，属正常现象，用等权+纪律换仓可消解。
4. **样本外衰减已实测**：2026 前 8 个月有成本超额 +6.5%，拉长到 1.7 年（含 2025 大涨市）衰减为 +1.0%。**不要用 8 个月结果外推长期收益**。
5. **训练配置一致性**：固化最优配置为 `--best --daily`（long_train=True，2016-2024 训练）。不带 `--long-train` 的 `--daily --model lgb360 --label20` 用的是短训练模型，程序会打印警告。
6. **涨跌停过滤（已修复双重静默失效，2026-09-16 审计）**：此前版本有两层 bug——(a) 公式误用 close/open 日内振幅而非当日涨幅；(b) index 对齐错误（D.features 返回 `(instrument, datetime)` 双层索引，与单层 isin 永远不匹配）导致**过滤从始至终未生效**，"已剔除 0 只"是假象。已修复为"当日涨幅 = close/前收 − 1，|涨幅|≥9.5%"并对齐索引层，实证 09-16 正确剔除 7 只。创业板/科创板 20% 涨跌幅股票会被 9.5% 阈值提前剔除——保守方向，可接受。
7. **模型只提供排序**：不提供仓位、止损、择时。仓位/风控规则见"策略落地操作手册"。

## 代码审计记录（2026-09-16）

发现并修复 3 个**静默失效型** bug（不崩溃、给错结果，最伤钱的类型），全部位于实盘信号输出路径（`daily_signal`），不影响研究路径：

| Bug | 影响 | 修复与验证 |
|---|---|---|
| 涨跌停过滤公式错（close/open） | 误剔除非涨停的高振幅股 | 改为 close/前收；与 `$change` 口径逐日计数 100% 一致 |
| 涨跌停过滤 index 对齐错 | **过滤从未生效**，榜单可能含不可买股票 | 取 instrument 层后再 isin；09-16 实证剔除 7 只 |
| `--daily` 不带 `--long-train` | 实盘信号用了短训练模型（与验证配置不一致） | 运行时警告 + 固化入口 `--best --daily` |

**影响面结论**：所有训练、回测、IC、参数选择（含超参搜索、walk-forward、负面结论实验）走 QLib 训练/回测引擎路径，不经过出 bug 的过滤代码，且日志留有 `long_train=True` 参数证据——**历史研究结论全部有效，无需重跑**。仅两份实盘榜单（09-11、09-16）作废并已用修复后配置重新生成（与旧榜单重合度仅 58%）。

**审计工具**：`modal run modal_qlib_cn_a10g.py::verify_integrity`——验证 `$change` 语义（即当日涨幅，偏差为复权舍入噪声）、两口径逐日涨跌停计数一致性、每日涨停股数量。**建议每次实盘建仓前跑一次**。

## 策略落地操作手册（把模型嵌入实盘框架）

### 模型的能力边界

模型只提供**选股排序**：每个交易日对 csi500 每只股票输出"未来 20 日收益"的相对预测分数（`--daily` 输出 top50 候选池）。

**它不提供**：总仓位建议、个股止损线、买卖时点、大盘择时——这些都需要你自己的规则。

### 月度操作流程（推荐框架）

```
每月初（或每 20 个交易日）：
1. 跑每日信号（固化入口，自动带 --long-train）：
   modal run modal_qlib_cn_a10g.py --best --daily --topk 50
2. 大盘门控：中证500 是否在 MA20 上方？
   - 是 → 按计划仓位执行（建议总仓位 50-70%，不强求满仓）
   - 否 → 减到半仓或空仓（择时与选股分离，用简单技术规则即可）
3. 建仓：等权买入榜单中不在当前持仓的股票（top50 每只约 2%）
4. 持有期间：
   - 单票跌破 -8%~-10% → 止损卖出（独立于模型的风险纪律，必须执行）
   - 可选：每 5 个交易日重跑榜单，分数跌出 top100 的提前换出
5. 到期：20 个交易日重新选池，循环
```

### 仓位与风控要点

- **总仓位**：IC 0.1 属弱信号，不建议满仓；建议 50-70% + 现金缓冲，或用大盘门控动态调节
- **个股仓位**：top50 等权最稳（分数差异小，按分数加权意义不大）
- **止损纪律**：单票 -8%~-10% 硬止损，防黑天鹅
- **先小仓位跟踪 2-4 周**：用真实数据验证执行细节（数据更新时点、榜单稳定性、滑点成本），再逐步加仓